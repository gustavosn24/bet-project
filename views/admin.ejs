<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Painel do Administrador</title>
  <link rel="stylesheet" href="/css/admin.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #0f172a;
      color: #fff;
      padding: 20px;
    }

    h1 {
      margin-bottom: 20px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 30px;
    }

    th, td {
      border: 1px solid #334155;
      padding: 10px;
      text-align: center;
    }

    th {
      background: #1e293b;
    }

    tr:nth-child(even) {
      background: #020617;
    }

    a, button {
      padding: 6px 10px;
      text-decoration: none;
      border-radius: 4px;
      border: none;
      cursor: pointer;
      color: #fff;
    }

    .btn-add {
      background: #22c55e;
      margin-bottom: 20px;
      display: inline-block;
    }

    .btn-edit {
      background: #3b82f6;
    }

    .btn-delete {
      background: #ef4444;
    }

    form {
      display: inline;
    }
  </style>
</head>
<body>

  <h1>⚙️ Painel do Administrador</h1>

  <a href="/admin/jogos/novo" class="btn-add">➕ Adicionar Jogo</a>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Time Casa</th>
        <th>Time Fora</th>
        <th>Data</th>
        <th>Odds Casa</th>
        <th>Odds Empate</th>
        <th>Odds Fora</th>
        <th>Ações</th>
      </tr>
    </thead>

    <tbody>
      <% if (jogos.length === 0) { %>
        <tr>
          <td colspan="8">Nenhum jogo cadastrado</td>
        </tr>
      <% } %>

      <% jogos.forEach(jogo => { %>
        <tr>
          <td><%= jogo.id %></td>
          <td><%= jogo.time_casa %></td>
          <td><%= jogo.time_fora %></td>
          <td><%= jogo.data_jogo %></td>
          <td><%= jogo.odd_casa %></td>
          <td><%= jogo.odd_empate %></td>
          <td><%= jogo.odd_fora %></td>
          <td>
            <a href="/admin/jogos/editar/<%= jogo.id %>" class="btn-edit">✏️ Editar</a>

            <form action="/admin/jogos/deletar/<%= jogo.id %>" method="POST">
              <button type="submit" class="btn-delete"
                onclick="return confirm('Deseja realmente excluir este jogo?')">
                🗑️ Deletar
              </button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>

</body>
</html>
